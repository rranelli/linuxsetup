- name: "installs deluge & deluge-web"
  apt:
    pkg={{ item }}
    state=installed
    update_cache=true
    cache_valid_time=86400
  with_items:
    - deluged
    - deluge-web
    - deluge-console
  notify:
    - restart deluge-web
    - restart deluged

- file: path=/home/renan/.config/deluge state=directory owner=renan
- name: "set deluge auth config file"
  copy:
    src=auth
    dest=/home/renan/.config/deluge/auth
    mode=0600
