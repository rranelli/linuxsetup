- name: "Desktop setup"
  hosts: 127.0.0.1
  connection: local
  become: yes
  roles:
    - role: facts
      tags: always
    - role: lastpass
      tags: lastpass
    - packages
    - ssh
    - apps
    - mimipass
    - code
    - role: emacs
      tags: emacs
    - docker
    - langtool
    - role: postgresql
      tags: postgresql
    - ruby
    - nodejs
    - python
    - elixir
  vars_files:
    - vars/secrets.yml
